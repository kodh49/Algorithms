fn main() {
    let year = 2021;
    // One of the Rust macros. Not a function
    println!("Hello World, {}", year);
}


// rustc {filename}.rs << Compile the file
// ./{filename} << Execute the file
