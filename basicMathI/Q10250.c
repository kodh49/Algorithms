#include <stdio.h>
#include <stdlib.h>

int main()
{
    int T;
    scanf("%d", &T);
    for (int i = 0; i < T; i++)
    { // iterate through different cases
        int k, n;
        scanf("%d\n", &k);
        scanf("%d\n", &n);
        prevStory = malloc(sizeof(int) * n);
        currStory = malloc(sizeof(int) * n);
        for (int j = 0; j < k; j++)
        { // updating each value in a specific story through for-loop
            for (int l = 0; l < n; l++)
            {
            }
        } // iterate until the row right below the k-th story
    }
    return 0;
}